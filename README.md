# 📋 Руководство по установке и запуску Pedigree MVP

## 📌 Описание проекта

Pedigree MVP — это веб-приложение для построения генеалогических деревьев (генограмм) пациентов в медицинских исследованиях. Система позволяет:

- **Управление пациентами**: создание, редактирование, удаление, поиск по ФИО/СНИЛС
- **Построение родственных связей**: родитель-ребенок, братья/сестры, супруги
- **Визуализация генограмм**: интерактивные графы с поколениями
- **Аутентификация и роли**: исследователи, врачи, администраторы
- **Хранение медицинских данных**: вес, рост, заболевания, мутации и др.

---

## 🛠 Технологический стек

### Backend
- **FastAPI** — веб-фреймворк
- **SQLAlchemy** — ORM
- **PostgreSQL** — база данных
- **JWT** — аутентификация
- **Python 3.9+**

### Frontend
- **React** + **TypeScript**
- **D3.js** — визуализация графов
- **Axios** — HTTP-клиент

---

## 📦 Требования к системе

- Python 3.9 или выше
- Node.js 16+
- PostgreSQL 12+
- pip (менеджер пакетов Python)
- npm (менеджер пакетов Node.js)

---

## 📄 Файл `requirements.txt`

```txt
# Основные зависимости
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
python-dotenv==1.0.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic==2.5.0
pydantic-settings==2.1.0

# Дополнительные
alembic==1.13.1
python-multipart==0.0.6
httpx==0.25.1
email-validator==2.1.0

# Для разработки (опционально)
pytest==7.4.3
black==23.11.0
flake8==6.1.0
```

---

## 🚀 Полная инструкция по запуску

### Шаг 1: Подготовка окружения

#### 1.1 Клонирование проекта
```bash
git clone <repository-url>
cd pedigree-mvp
```

#### 1.2 Создание виртуального окружения Python
```bash
python -m venv venv

# Для Windows:
venv\Scripts\activate

# Для Linux/Mac:
source venv/bin/activate
```

#### 1.3 Установка Python-зависимостей
```bash
pip install -r requirements.txt
```

#### 1.4 Установка Node.js зависимостей
```bash
cd frontend
npm install
cd ..
```

### Шаг 2: Настройка базы данных PostgreSQL

#### 2.1 Установите PostgreSQL если не установлен:
- **Windows**: [PostgreSQL Downloads](https://www.postgresql.org/download/windows/)
- **Ubuntu/Debian**: `sudo apt install postgresql postgresql-contrib`
- **Mac**: `brew install postgresql`

#### 2.2 Создайте базу данных и пользователя:
```sql
-- Подключитесь к PostgreSQL:
-- sudo -u postgres psql

CREATE DATABASE pedigree_db;
CREATE USER pedigree_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE pedigree_db TO pedigree_user;
ALTER DATABASE pedigree_db OWNER TO pedigree_user;
```

#### 2.3 Настройте переменные окружения:
Создайте файл `.env` в корневой директории проекта:

```env
# Database
DATABASE_URL=postgresql://pedigree_user:your_password@localhost:5432/pedigree_db

# JWT
JWT_SECRET=your_super_secret_key_change_in_production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS (опционально)
ALLOWED_ORIGINS=http://localhost:3000
```

### Шаг 3: Инициализация базы данных

#### 3.1 Запустите SQL скрипт для создания таблиц:
```bash
# Вариант 1: Используя psql
psql -U pedigree_user -d pedigree_db -f create_tables.sql

# Вариант 2: Через Python
python backend/init_db.py  # Если такой файл существует
```

#### 3.2 Проверьте создание таблиц:
```sql
\c pedigree_db
\dt
```

### Шаг 4: Запуск бэкенда

#### 4.1 Из корневой директории:
```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

#### 4.2 Проверьте работу API:
- Откройте в браузере: `http://localhost:8000/docs` (Swagger UI)
- Или: `http://localhost:8000/redoc` (ReDoc)

### Шаг 5: Запуск фронтенда

#### 5.1 В новом терминале:
```bash
cd frontend
npm start
# или
npm run dev
```

#### 5.2 Фронтенд будет доступен по адресу:
`http://localhost:3000`

---

## 🔧 Конфигурация приложения

### Структура проекта:
```
pedigree-mvp/
├── backend/
│   ├── main.py              # Точка входа FastAPI
│   ├── database.py          # Настройки БД
│   ├── models.py           # SQLAlchemy модели
│   ├── schemas.py          # Pydantic схемы
│   ├── crud.py             # Операции с БД
│   ├── auth.py             # Аутентификация
│   └── requirements.txt    # Python зависимости
├── frontend/
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   ├── api.ts         # API клиент
│   │   └── App.tsx        # Главный компонент
│   ├── package.json       # Node.js зависимости
│   └── public/
├── create_tables.sql      # SQL для создания таблиц
├── .env                  # Переменные окружения
└── README.md            # Эта документация
```

### Роли пользователей:
1. **researcher** (по умолчанию) — может добавлять/редактировать пациентов
2. **doctor** — просмотр данных
3. **admin** — полный доступ + управление пользователями

---

## 🧪 Тестовые данные

### Создание первого пользователя (через API):
```bash
curl -X POST "http://localhost:8000/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "admin123",
    "full_name": "Администратор",
    "role": "admin"
  }'
```

### Логин:
```bash
curl -X POST "http://localhost:8000/token" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin@example.com&password=admin123"
```

---

## ⚠️ Устранение неполадок

### 1. Ошибка подключения к БД:
```
sqlalchemy.exc.OperationalError: (psycopg2.OperationalError)
```
**Решение:** Проверьте:
- PostgreSQL запущен: `sudo systemctl status postgresql`
- Пароль в `.env` правильный
- Пользователь имеет права на базу данных

### 2. CORS ошибки:
```
Access-Control-Allow-Origin
```
**Решение:** Убедитесь что фронтенд запущен на `localhost:3000` и добавлен в `origins` в `main.py`

### 3. Модули не найдены:
```
ModuleNotFoundError: No module named 'fastapi'
```
**Решение:** Активируйте виртуальное окружение и переустановите зависимости:
```bash
source venv/bin/activate  # или venv\Scripts\activate
pip install -r requirements.txt
```

### 4. React не запускается:
```
Error: Cannot find module 'react'
```
**Решение:**
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
```

---

## 📊 API эндпоинты

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/register` | Регистрация пользователя |
| POST | `/token` | Получение JWT токена |
| GET | `/patients` | Список пациентов |
| POST | `/patients` | Создание пациента |
| GET | `/patients/{id}` | Получение пациента |
| PUT | `/patients/{id}` | Обновление пациента |
| DELETE | `/patients/{id}` | Удаление пациента |
| POST | `/relations` | Добавление родственной связи |
| POST | `/links` | Добавление связи (братья/супруги) |
| GET | `/pedigree/{id}` | Получение генограммы |

---

## 🔒 Безопасность

1. **В продакшене:**
   - Измените `JWT_SECRET` на сложную случайную строку
   - Используйте HTTPS
   - Настройте CORS для конкретных доменов
   - Используйте переменные окружения для чувствительных данных

2. **Рекомендации:**
   - Регулярно обновляйте зависимости
   - Настройте брандмауэр
   - Используйте read-only пользователя для фронтенда

---

## 📝 Дополнительные команды

### Миграции базы данных (если используется Alembic):
```bash
alembic init migrations
alembic revision --autogenerate -m "Initial migration"
alembic upgrade head
```

### Запуск тестов:
```bash
# Бэкенд
pytest backend/tests/

# Фронтенд
cd frontend
npm test
```

### Сборка для продакшена:
```bash
# Фронтенд
cd frontend
npm run build

# Бэкенд (с помощью Docker)
docker build -t pedigree-backend .
docker run -p 8000:8000 pedigree-backend
```

---

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи сервера: `uvicorn main:app --reload`
2. Проверьте логи React: открыть DevTools (F12)
3. Убедитесь что все сервисы запущены:
   - PostgreSQL
   - Backend (порт 8000)
   - Frontend (порт 3000)

Для дополнительной помощи обратитесь к документации:
- [FastAPI документация](https://fastapi.tiangolo.com/)
- [React документация](https://react.dev/)
- [SQLAlchemy документация](https://docs.sqlalchemy.org/)

---

## 📄 Лицензия

Проект предназначен для образовательных и исследовательских целей. Перед использованием в продакшене проконсультируйтесь с экспертами по безопасности и соответствию нормативным требованиям (например, GDPR, HIPAA для медицинских данных).

---

**🚀 Приложение готово к использованию!** Откройте `http://localhost:3000` в браузере и начните работу с генограммами пациентов.
